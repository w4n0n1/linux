curl ipinfo.io/ip
whatweb (url)
whatweb -v (url)
whatweb --log-verbose=nombredepagina_Report (url) # exporta los resultados a un archivo de texto
amass enum --passive -d (dominio) -o (archivo.txt)
dirb (http://url/) (diccionario) #ataque de fuerza bruta a directorios web
lbd (dominio) # para buscar balanceadores de carga
dirbuster # GUI - busqueda de directorios con diccionario
